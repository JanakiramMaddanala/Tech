// let div = document.querySelector('div');

// div.className = 'main etc';
// div.classList.add('div');
// // div.classList.remove('div');
// console.log(div);
// console.log(div.className);

// document.body.style.display = "none";
// document.body.style.cssText = "display:none;background-color:gray;"

// setTimeout(() => document.body.style.display = "", 5000);

// document.body.style.padding = '100px';
// let h1 = document.querySelector('h1');
// let h1Style = getComputedStyle(h1);

// console.log(h1Style.color);

// function showNotification(opt){
//     let notificationDiv = document.createElement('div');

//     notificationDiv.innerHTML = opt.html;
//     notificationDiv.style.top = opt.top + 'px';
//     notificationDiv.style.right = opt.right + 'px';
//     notificationDiv.classList.add(opt.className);
    
//     document.body.append(notificationDiv);

//     setTimeout(() => notificationDiv.remove(), 1500);
// }

// let options = {
//     top: 10, // 10px from the top of the window (by default 0px)
//     right: 10, // 10px from the right edge of the window (by default 0px)
//     html: "Hello!", // the HTML of notification
//     className: "welcome" // an additional class for the div (optional)
// };

// setInterval(() => showNotification(options), 2000)

