-- AND
SELECT *
FROM [dbo].[Person]
WHERE 8 <> ALL (SELECT 9 UNION SELECT 2)

GO
-- OR
SELECT *
FROM [dbo].[Person]
WHERE 8 = ANY (SELECT 9 UNION SELECT 8)

GO 
SELECT *
FROM [dbo].[Person]
WHERE 8 = SOME (SELECT 8 UNION SELECT 9)

GO

SELECT ROW_NUMBER() OVER(ORDER BY (SELECT 1))

INSERT INTO [dbo].[Person]
VALUES (3, 'SI', 2000)

SELECT TOP(70) PERCENT *
FROM [dbo].[Person]


